# Windows 远程桌面全流程排坑指南

> 从系统原地升级专业版，到解决远程桌面“凭据无效”之谜，再到4K高清优化，一次讲透。

## 一、原地升级：Windows 10 家庭版 → 专业版

### 原理
Windows 安装镜像本身包含所有版本（家庭版/专业版/企业版/教育版）的完整文件。产品密钥决定了“解锁”哪个版本。更换密钥即可原地切换，无需重装系统。

### 操作步骤
1.  **断网**：拔掉网线或在网络设置中暂时禁用网络连接。
2.  **打开激活设置**：按 `Win + R`，输入 `slui.exe` 回车，或进入「设置 → 更新和安全 → 激活」。
3.  **更改产品密钥**：点击“更改产品密钥”，输入通用升级密钥：
    ```
    VK7JG-NPHTM-C97JM-9MPGT-3V66T
    ```
4.  **等待升级**：系统提示“正在准备升级”，耐心等待自动重启完成。
5.  **恢复网络**：重启后重新连接网络。
6.  **激活**：升级后系统处于未激活状态，使用激活工具完成激活（如 MAS 开源脚本等，此处不展开）。

### 验证
右键「此电脑」→「属性」，查看“Windows 规格”中显示为“Windows 10 专业版”即为成功。

---

## 二、远程桌面登录：macOS → Windows

### 场景
- **主控端**：MacBook（macOS），使用微软官方远程桌面客户端 **Windows App**（原 Microsoft Remote Desktop）
- **被控端**：Windows 台式机，IP `172.27.179.171`，主机名 `desktop-o277hmd`
- **登录方式**：Windows 端使用 Outlook 邮箱（微软账户）登录

### 找出真正的本地用户名
Windows 在使用微软账户登录时，会自动生成一个缩短的本地用户名。远程桌面需要的是这个本地用户名，而不是邮箱地址。

**在被控的 Windows 电脑上操作：**
1. 按 `Win` 键，输入 `cmd`，打开命令提示符。
2. 输入以下命令并回车：
   ```cmd
   whoami
   ```
3. 显示结果为 `desktop-o277hmd\jeese`，其中 **`jeese`** 就是本地用户名。

### 填坑：凭据无效的真正原因

以下路径最终证明是 **错误方向**，放在这里是为了提醒以后不再白费力气。

#### ❌ 误区 1：用户名格式问题
曾尝试以下格式，均无效：
- `.\jeese`
- `desktop-o277hmd\jeese`
- `MicrosoftAccount\邮箱地址`
- 单纯使用邮箱地址

#### ❌ 误区 2：凭据缓存问题
尝试通过命令行 `runas /user:MicrosoftAccount\邮箱地址 notepad` 强制缓存凭据，并重启 `Remote Desktop Services` 服务，均无效。

#### ❌ 误区 3：NLA 网络级身份验证
在 `sysdm.cpl` 的“远程”选项卡中取消勾选“仅允许运行使用网络级别身份验证的远程桌面的计算机连接”，问题依旧。

#### ❌ 误区 4：安全策略与组策略
调整本地安全策略 (`secpol.msc`) 和组策略 (`gpedit.msc`) 中的“凭据分配”、“共享和安全模型”等设置，均未解决。

### ✅ 根本原因与最终解决

**真相：单会话限制**
Windows 10/11 专业版（非服务器版）只允许 **一个用户拥有一个活动桌面会话**。

当时 Windows 笔记本已经用 `jeese` 账户登录并占据桌面，当 Mac 再以同一账户远程连接时，Windows 认为这是“抢占会话”，拒绝连接，并弹出误导性提示 **“凭据无效”**。

**解决步骤：**
1.  走到 Windows **笔记本**前。
2.  **选择“注销”或“重启”**（锁定屏幕不行！），确保笔记本停留在输入密码的登录界面。
3.  回到 Mac，打开 **Windows App**，填入：
    - **用户名**：**`jeese`**（仅此即可，无需任何前缀！）
    - **密码**：Outlook 邮箱密码
4.  连接成功。

> **关键教训：** 系统提示“凭据无效”不代表用户名或密码真的错误，很可能是 **会话冲突** 导致的误导。**注销被控机上的同一账户，是最简单直接的终极解法**。

---

## 三、4K 显示器远程桌面模糊优化

### 核心调节：macOS 端的 Windows App 设置
1.  在 Mac 的 Windows App 中，右键目标电脑，选择“编辑”。
2.  切换到 **“显示”** 选项卡：
    -   ✅ 勾选 **“针对Retina显示屏优化”**
    -   将 **“颜色质量”** 设置为 **32 位**
3.  重新连接即可显著改善清晰度。

> 备选方案：若勾选 Retina 优化后图标太小，可关闭该选项，并将分辨率设为 **2560 x 1440**。

### 补充优化：Windows 端的组策略设置
在 Windows 电脑上按 `Win + R`，输入 `gpedit.msc`，依次展开：
`计算机配置 → 管理模板 → Windows组件 → 远程桌面服务 → 远程桌面会话主机 → 远程会话环境`

将以下三项全部设为 **“已启用”**：
-   **为所有远程桌面服务会话使用硬件默认的图形适配器**
-   **配置远程桌面服务的颜色深度**（下方选择“32位每像素”）
-   **启用远程桌面服务字体平滑**

修改后以管理员身份运行 `cmd`，执行 `gpupdate /force` 使其立即生效。

---

## 最终避坑清单

1.  **远程桌面提示“凭据无效” ≠ 用户名或密码错误**
    先检查是否同一账户已在被控机登录。**注销被控机，是解决此类问题的首选操作。**

2.  **Windows 版本切换不需要重装**
    系统本身具备一整套完整功能，密钥只是钥匙。理解这一点，可以避免很多重复工作。

3.  **设置改动后不生效，先重启服务或注销**
    许多 Windows 配置（尤其远程与安全策略）不会即时生效，**注销当前会话或重启相关服务**能解决大部分“明明设置了却没用”的问题。

4.  **微软账户远程登录，请用本地用户名**
    登录用的是缩短后的本地账户名（通过 `whoami` 查询），而非完整的邮箱地址。

5.  **远程桌面客户端设置比主机端优先级更高**
    画面模糊等问题，优先检查发起连接的客户端（如 Mac 的 Windows App）的显示设置，往往能最快解决问题。
